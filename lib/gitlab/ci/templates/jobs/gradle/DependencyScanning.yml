gemnasium-maven-dependency_scanning:
  artifacts:
    reports:
      dependency_scanning:
        - gl-dependency-scanning-report.json
    paths:
      - gl-dependency-scanning-report.json
  rules:
    - if: $MAVEN_DETECTION_DISABLED
      when: never
    - if: $CI_COMMIT_TAG
      when: never
    - if: $CI_MERGE_REQUEST_IID
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_BRANCH =~ $DEV_OR_RELEASE_REGEX
    - if: $CI_PIPELINE_SOURCE == "web"


dependency_scanning_validation:
  stage: test
  image: "${IMAGE_PREFIX}python:3.11-rc-alpine"
  allow_failure: true
  needs: ["gemnasium-maven-dependency_scanning"]
  script:
    - ls
    - test -e gl-dependency-scanning-report.json || exit 0
    - wget https://github.com/chesapeaketechnology/gitlab-templates/tree/bugfix/Issue-22-fix-broken-quality-report-linkslib/gitlab/ci/templates/references/security/validate_security_scan.py
    - python3 validate_security_scan.py gl-dependency-scanning-report.json $EXCLUDED_VULNERABILITY_PACKAGES
  rules:
    - if: $MAVEN_DETECTION_DISABLED
      when: never
    - if: $CI_COMMIT_TAG
      when: never
    - if: $CI_MERGE_REQUEST_IID
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_BRANCH =~ $DEV_OR_RELEASE_REGEX
    - if: $CI_PIPELINE_SOURCE == "web"