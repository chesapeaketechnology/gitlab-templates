variables:
  HELM_IMAGE: "chesapeaketechnology/devops"
  HELM_CHART_DIR: ""

helmLint:
  stage: test
  image: $HELM_IMAGE
  script:
    - echo '* Running a series of tests to verify that the chart is well-formed'
    - helm lint $HELM_CHART_DIR